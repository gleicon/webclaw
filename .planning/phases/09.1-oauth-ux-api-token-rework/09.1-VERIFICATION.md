---
phase: 09.1-oauth-ux-api-token-rework
verified: 2026-03-09T00:00:00Z
status: passed
score: 19/19 must-haves verified
---

# Phase 09.1: OAuth UX / API Token Rework — Verification Report

**Phase Goal:** Rework OAuth UX and API token handling — add PAT support for GitHub and Notion, separate card variants for PAT vs OAuth providers, auth-type badges, visible redirect URI callouts, and masked-after-save token display. Extend Go/WASM layer with SavePAT, MarkInvalid, Valid and AuthType fields on ConnectionStatus.

**Verified:** 2026-03-09
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Token struct has `AuthType` field ("pat" or empty=oauth) | VERIFIED | `internal/oauth/token_store.go` line 23: `AuthType string \`json:"auth_type,omitempty"\`` |
| 2  | `OAuthManager.SavePAT()` stores a Token with `AuthType="pat"`, zero ExpiresAt, empty RefreshToken | VERIFIED | `internal/oauth/manager.go` lines 108-126: SavePAT method with `AuthType: "pat"` and `time.Time{}` ExpiresAt |
| 3  | `OAuthManager.GetToken()` returns PAT immediately, skipping all refresh logic | VERIFIED | `manager.go` lines 255-263: early return on `token.AuthType == "pat"` before NeedsRefresh check |
| 4  | `OAuthManager.InitiateConnection()` returns error for 'github' and 'notion' | VERIFIED | `manager.go` lines 47-51: guard returns error before `GetProvider` call |
| 5  | `OAuthManager.MarkInvalid()` sets transient invalid flag; `GetConnectionStatus` returns `Valid=false` | VERIFIED | `manager.go` lines 128-138: MarkInvalid sets `m.invalidTokens[provider]=true`; lines 430-431: `Valid: !m.invalidTokens[providerName]` |
| 6  | `ConnectionStatus` has `Valid bool` and `AuthType string` fields | VERIFIED | `manager.go` lines 444-447: both fields present with JSON tags |
| 7  | `webclaw.oauth.savePATToken(provider, pat)` JS export exists, returns Promise | VERIFIED | `internal/oauth/js_exports.go` lines 120-146: goroutine-in-Promise pattern, registered on `oauthObj` |
| 8  | `webclaw.oauth.markInvalid(provider)` JS export exists, synchronous | VERIFIED | `js_exports.go` lines 149-158: direct synchronous call, returns `js.Undefined()` |
| 9  | WASM binary rebuilt and at `static/webclaw.wasm` | VERIFIED | `static/webclaw.wasm` exists (6.5 MB, dated 2026-03-07) |
| 10 | GitHub card shows PAT input, no Connect button | VERIFIED | `index.html` `buildPATCard()` creates `#oauth-pat-input-github` but does not create `#oauth-btn-github`; grep for `oauth-btn-github` returns no results |
| 11 | Notion card shows PAT input, no Connect button | VERIFIED | Same `buildPATCard()` path; `#oauth-btn-notion` absent |
| 12 | Twitter and Google cards show permanently-visible redirect URI callout | VERIFIED | `index.html` lines 1808-1833: `#oauth-redirect-callout-{id}` created with `textContent = "about:blank"` |
| 13 | All four cards have auth-type badges with correct labels | VERIFIED | OAuth cards: `badge.textContent = "OAuth 2.0"` (line 1684); PAT cards: `badge.textContent = "Access Token"` (line 1917) |
| 14 | After PAT save, input clears and masked state shows | VERIFIED | `index.html` lines 2028-2046: optimistic clear + `showMaskedState()` called synchronously before async save; reverted on error |
| 15 | Status indicator shows green/gray/red dot for connected/not-connected/invalid | VERIFIED | `updateAllConnectionStatus()` lines 2202-2238: three branches using `setStatusDot()` helper |
| 16 | `updateAllConnectionStatus()` handles `valid` and `auth_type` fields | VERIFIED | Lines 2203 (`status.valid === false`) and 2214 (`status.auth_type === "pat"`) — both branches present and wired |
| 17 | All DOM manipulation uses `createElement + textContent + appendChild` (no innerHTML with variable content) | VERIFIED | Connected Services section (lines 1560-2260) has zero `innerHTML` assignments; `setStatusDot()` helper uses `createTextNode`; disconnect modal uses `createElement` throughout |
| 18 | Playwright test scaffold exists and is discoverable | VERIFIED | `test/phase09.1-e2e/01-connected-services-smoke.spec.mjs` (8 tests) and `02-pat-save-flow.spec.mjs` (5 tests, 1 intentionally skipped) present; `playwright.config.mjs` uses `testMatch` glob covering both suites |
| 19 | Playwright smoke tests (8 DOM assertions) and PAT flow tests (4 runnable) pass | VERIFIED per SUMMARY | Plan 03 SUMMARY reports 8/8 smoke green, 4/4 PAT flow green (test 13 skipped by design); human checkpoint approved |

**Score:** 19/19 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/oauth/token_store.go` | Token struct with `AuthType` field | VERIFIED | Line 23: `AuthType string \`json:"auth_type,omitempty"\`` |
| `internal/oauth/manager.go` | `SavePAT`, `MarkInvalid`, `ClearInvalid` methods; `GetToken` PAT guard; `InitiateConnection` guard; `ConnectionStatus.Valid` and `AuthType` | VERIFIED | All methods present; `invalidTokens map` in struct; all guards in place |
| `internal/oauth/js_exports.go` | `savePATToken` (async) and `markInvalid` (sync) exports registered on `oauthObj` | VERIFIED | Lines 120-158 |
| `static/webclaw.wasm` | Rebuilt WASM binary with new exports | VERIFIED | File exists, 6.5 MB, dated 2026-03-07 after Go source changes |
| `index.html` | `buildPATCard`, `buildOAuthCard`, `setStatusDot`, `showMaskedState` helpers; `OAUTH_PROVIDERS` with `authType` field; all required element IDs | VERIFIED | All helpers confirmed; element IDs present as dynamic `"oauth-*-" + provider.id` strings |
| `static/main.css` | Compiled Tailwind CSS including new classes | VERIFIED | File exists, dated 2026-03-08 |
| `test/phase09.1-e2e/01-connected-services-smoke.spec.mjs` | 8 DOM smoke tests | VERIFIED | File present with 8 named tests |
| `test/phase09.1-e2e/02-pat-save-flow.spec.mjs` | 5 PAT flow tests (1 intentionally skipped) | VERIFIED | File present with 5 tests; test 13 uses `test.skip(true, ...)` with documented manual path |
| `test/playwright.config.mjs` | Discovers both phase06 and phase09.1 suites | VERIFIED | `testDir: '.'` with `testMatch` glob covering both directories |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `js_exports.go` | `manager.go` | `m.SavePAT()` and `m.ClearInvalid()` calls | WIRED | Lines 133, 139 in js_exports.go |
| `js_exports.go` | `manager.go` | `m.MarkInvalid()` call | WIRED | Line 155 in js_exports.go |
| `manager.go:GetToken` | `token.AuthType` | Early return branch `token.AuthType == "pat"` | WIRED | Lines 258-263 in manager.go |
| `manager.go:GetConnectionStatus` | `invalidTokens` map | `Valid: !m.invalidTokens[providerName]` | WIRED | Line 431 in manager.go |
| `index.html buildConnectedServicesUI()` | `webclaw.oauth.savePATToken` | `patSaveBtn` click handler | WIRED | Lines 2034-2035 in index.html |
| `index.html updateAllConnectionStatus()` | `status.valid` | Red-dot branch on `status.valid === false` | WIRED | Line 2203 in index.html |
| `index.html` | `oauth-badge-{id}` | Badge element created in both `buildOAuthCard` and `buildPATCard` | WIRED | Lines 1682, 1915 |
| `index.html` | `oauth-redirect-callout-{id}` | Callout element in `buildOAuthCard` with `textContent = "about:blank"` | WIRED | Lines 1809-1833 |
| `test/playwright.config.mjs` | `test/phase09.1-e2e/` | `testMatch` glob `**/phase09.1-e2e/**/*.spec.mjs` | WIRED | Line 9 in playwright.config.mjs |

---

### Requirements Coverage

All requirements declared across plans were satisfied:

| Requirement | Source Plan | Status |
|-------------|-------------|--------|
| AUTH-UX-01 through AUTH-UX-06 | 09.1-01 (scaffold), 09.1-03 (UI) | SATISFIED — DOM smoke tests present and passing; card structure implemented |
| AUTH-UX-07 through AUTH-UX-11 | 09.1-02 (Go backend) | SATISFIED — SavePAT, MarkInvalid, GetToken guard, InitiateConnection guard, ConnectionStatus.Valid all implemented |

---

### Anti-Patterns Found

No blocking anti-patterns detected in the Connected Services implementation:

- Zero `innerHTML` assignments in the Connected Services JS section (lines 1560-2260)
- No TODO/FIXME/placeholder comments in modified files
- No empty handlers — all save/toggle/update/disconnect flows have real implementations
- `test.skip(true, ...)` on test 13 (markInvalid auto-refresh) is intentional and documented with a manual verification path — not a stub

---

### Human Verification Required

The following were verified at human checkpoint (Task 3, Plan 03 SUMMARY records approval):

1. **GitHub card visual inspection** — PAT input with "ghp_..." placeholder, scopes hint, creation link, "Access Token" badge, Save producing masked state
2. **Notion card visual inspection** — token input, yellow page-sharing callout, creation link, "Access Token" badge
3. **Twitter card visual inspection** — Client ID field, always-visible "Callback URL: about:blank" callout with Copy button, "OAuth 2.0" badge, Connect button
4. **Google card visual inspection** — same pattern as Twitter
5. **Browser console verification** — `typeof window.webclaw.oauth.savePATToken === "function"` and `typeof window.webclaw.oauth.markInvalid === "function"` confirmed

Human checkpoint was recorded as APPROVED in Plan 03 SUMMARY (`09.1-03-SUMMARY.md`).

---

## Summary

Phase 09.1 achieved its goal. All three waves completed cleanly:

- **Wave 0 (Plan 01):** Playwright test scaffold with 13 tests (8 smoke + 5 PAT flow) driving DOM contracts.
- **Wave 1 (Plan 02):** Go/WASM layer extended with `AuthType` on Token, `SavePAT`/`MarkInvalid`/`ClearInvalid` methods, PAT guards in `GetToken` and `InitiateConnection`, `Valid`/`AuthType` on `ConnectionStatus`, and two new JS exports. WASM rebuilt.
- **Wave 2 (Plan 03):** `index.html` Connected Services section fully reworked with `buildOAuthCard`/`buildPATCard` card variants, `setStatusDot` safe DOM helper, auth-type badges, redirect URI callouts, and three-state status rendering. CSS compiled. All Playwright tests green.

No gaps found. Phase goal is fully achieved.

---

_Verified: 2026-03-09_
_Verifier: Claude (gsd-verifier)_
