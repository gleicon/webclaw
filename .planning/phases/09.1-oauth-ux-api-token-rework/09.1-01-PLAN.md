---
phase: 09.1-oauth-ux-api-token-rework
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/phase09.1-e2e/01-connected-services-smoke.spec.mjs
  - test/phase09.1-e2e/02-pat-save-flow.spec.mjs
autonomous: true
requirements: [AUTH-UX-01, AUTH-UX-02, AUTH-UX-03, AUTH-UX-04, AUTH-UX-05, AUTH-UX-06]

must_haves:
  truths:
    - "Test files exist in test/phase09.1-e2e/ and Playwright can discover them"
    - "Smoke tests verify card DOM structure for all four providers"
    - "PAT flow tests verify save + masked state behavior using mocked webclaw.oauth"
    - "All tests start in PENDING/SKIP state before implementation (no false positives)"
  artifacts:
    - path: "test/phase09.1-e2e/01-connected-services-smoke.spec.mjs"
      provides: "DOM smoke tests for card structure, badges, callouts"
    - path: "test/phase09.1-e2e/02-pat-save-flow.spec.mjs"
      provides: "PAT save flow, masked state, invalid token state tests"
  key_links:
    - from: "test/playwright.config.mjs"
      to: "test/phase09.1-e2e/"
      via: "testDir or project config"
      pattern: "phase09.1-e2e"
---

<objective>
Create the Phase 9.1 Playwright test scaffold before any implementation begins. Tests are DOM-assertion-only — they assert specific element IDs, text content, and card structure that the implementation plans will create. Tests are written to FAIL until implementation is complete (Nyquist compliance).

Purpose: Establishes automated verify commands for all subsequent tasks. Playwright cannot discover tests that do not exist, so every task in Plans 02 and 03 depends on this scaffold being present.

Output: Two Playwright spec files covering all 13 behavioral requirements from VALIDATION.md, plus a Playwright config that includes the new test directory.
</objective>

<execution_context>
@/Users/gleicon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gleicon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-oauth-ux-api-token-rework/09.1-CONTEXT.md
@.planning/phases/09.1-oauth-ux-api-token-rework/09.1-VALIDATION.md

<interfaces>
<!-- Playwright test conventions from existing phase06-browser-tests -->

From test/phase06-browser-tests/helpers.js (pattern):
```javascript
// All tests wait for WASM to be ready before asserting
await page.waitForFunction(() => window.webclaw && window.webclaw.oauth);
// Element IDs used in existing Connected Services cards
// #oauth-provider-{id}   — provider card wrapper row
// #oauth-btn-{id}        — connect/disconnect button
// #oauth-status-{id}     — status text span
// #oauth-clientid-{id}   — client ID input
// #oauth-config-{id}     — config row (containing client ID input)
```

From test/playwright.config.mjs (current config):
```javascript
export default defineConfig({
  testDir: './phase06-browser-tests',
  // webServer starts go run ./cmd/devserver/ on port 8080
  // workers: 1 (sequential)
  // projects: chromium only
});
```

New element IDs that implementation will create (drive tests from these contracts):
// #oauth-badge-{id}        — auth-type badge per card
// #oauth-pat-input-{id}    — PAT/token password input (github, notion)
// #oauth-pat-save-{id}     — Save button for PAT
// #oauth-pat-toggle-{id}   — Show/hide toggle for PAT input
// #oauth-pat-update-{id}   — Update token button (shown when masked)
// #oauth-redirect-callout-{id} — redirect URI callout (twitter, google)
// #oauth-masked-{id}       — masked token display container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/phase09.1-e2e/ with DOM smoke spec</name>
  <files>test/phase09.1-e2e/01-connected-services-smoke.spec.mjs</files>
  <action>
Create the directory test/phase09.1-e2e/ and write 01-connected-services-smoke.spec.mjs.

The test file must use ES module syntax (`.mjs`) matching existing test conventions.

Tests to include (all use `page.goto('/')` then navigate to Settings > Connected Services tab):

**Test suite: "Connected Services card structure (smoke)"**

Helper: Before each test, navigate to the Connected Services section. The Settings tab is accessible via a button with text "Settings" or id "tab-settings". Wait for `#connected-services-list` to be visible.

Do NOT wait for `window.webclaw` in smoke tests — these test DOM structure only, not live WASM behavior.

Tests:

1. `"GitHub card: PAT input present, no Connect button"` — Assert `#oauth-pat-input-github` is visible. Assert `#oauth-btn-github` does NOT exist (use `expect(page.locator('#oauth-btn-github')).toHaveCount(0)` or assert it has class `hidden`).

2. `"Notion card: token input present, no Connect button"` — Assert `#oauth-pat-input-notion` is visible. Assert `#oauth-btn-notion` does NOT exist.

3. `"Twitter card: Client ID field and redirect URI callout visible"` — Assert `#oauth-clientid-twitter` is visible. Assert `#oauth-redirect-callout-twitter` is visible. Assert callout text contains "about:blank".

4. `"Google card: Client ID field and redirect URI callout visible"` — Assert `#oauth-clientid-google` is visible. Assert `#oauth-redirect-callout-google` is visible. Assert callout text contains "about:blank".

5. `"Twitter card: auth-type badge shows OAuth 2.0"` — Assert `#oauth-badge-twitter` exists and has textContent "OAuth 2.0".

6. `"Google card: auth-type badge shows OAuth 2.0"` — Assert `#oauth-badge-google` exists and has textContent "OAuth 2.0".

7. `"GitHub card: auth-type badge shows Access Token"` — Assert `#oauth-badge-github` exists and has textContent "Access Token".

8. `"Notion card: auth-type badge shows Access Token"` — Assert `#oauth-badge-notion` exists and has textContent "Access Token".

Use `import { test, expect } from '@playwright/test';`. Use `page.locator()` for element selection. Use `test.describe()` to group.

For locating the Connected Services section: assume Settings tab click makes `#connected-services-list` visible. Use `await page.click('[data-tab="settings"]')` or the actual selector from the existing page — look for a tab button containing text "Settings", then wait for the connected services section.

These tests will FAIL until Plan 03 (index.html rework) is complete. That is correct behavior for Wave 0.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/webclaw/test && ls phase09.1-e2e/01-connected-services-smoke.spec.mjs</automated>
  </verify>
  <done>File exists. Content has at least 8 test cases. Playwright can parse the file without syntax errors (run `npx playwright test --list --config ./playwright.config.mjs` to confirm discovery if config is updated).</done>
</task>

<task type="auto">
  <name>Task 2: Create PAT save flow spec and update Playwright config</name>
  <files>
    test/phase09.1-e2e/02-pat-save-flow.spec.mjs
    test/playwright.config.mjs
  </files>
  <action>
Write 02-pat-save-flow.spec.mjs covering the PAT save and masked-state interactions.

**Test suite: "PAT save flow"**

These tests REQUIRE `window.webclaw.oauth` to be ready. Each test must first:
```javascript
await page.goto('/');
await page.waitForFunction(() => window.webclaw && window.webclaw.oauth && window.webclaw.oauth.savePATToken, { timeout: 10000 });
// Navigate to Settings > Connected Services
```

Tests:

1. `"GitHub: savePATToken export exists on webclaw.oauth"` — Use `page.evaluate()` to check `typeof window.webclaw.oauth.savePATToken === 'function'`. This tests WASM export presence, not UI.

2. `"markInvalid export exists on webclaw.oauth"` — Use `page.evaluate()` to check `typeof window.webclaw.oauth.markInvalid === 'function'`.

3. `"GitHub: entering PAT and saving shows masked state"` — Find `#oauth-pat-input-github`, fill it with `'ghp_TESTVALUE12345'`, click `#oauth-pat-save-github`, then wait for `#oauth-masked-github` to be visible OR for `#oauth-pat-input-github` to have attribute `type="password"` with empty value. Assert `#oauth-status-github` text contains "Connected".

4. `"Notion: entering token and saving shows masked state"` — Same pattern for Notion with `#oauth-pat-input-notion`, `#oauth-pat-save-notion`, `#oauth-masked-notion`, `#oauth-status-notion`.

5. `"Invalid token: markInvalid causes red dot on card"` — Call `await page.evaluate(() => window.webclaw.oauth.markInvalid('github'))`. Then trigger a connection status refresh (call `await page.evaluate(() => window.webclaw.oauth.getConnectionStatus())` — or wait for the UI to poll). Assert that `#oauth-status-github` contains "Invalid" OR has a CSS class indicating error state. Note: this test may be SKIP-worthy if the UI does not auto-refresh — add `test.skip()` with a comment explaining the manual verification path from VALIDATION.md.

**Update test/playwright.config.mjs:**

The existing config has `testDir: './phase06-browser-tests'`. This must be changed to run BOTH the phase06 tests AND the new phase09.1 tests. Two options:

Option A (preferred — zero risk to existing tests): Change `testDir` to `'.'` and add `testMatch` glob: `['phase06-browser-tests/**/*.spec.js', 'phase09.1-e2e/**/*.spec.mjs']`. This includes both suites.

Option B: Keep existing config unchanged and create `test/playwright.config.091.mjs` pointing at `./phase09.1-e2e`. Then update package.json scripts with a `test:091` entry.

Use Option A. Modify the existing `test/playwright.config.mjs`:
- Change `testDir: './phase06-browser-tests'` to `testDir: '.'`
- Add `testMatch: ['**/phase06-browser-tests/**/*.spec.js', '**/phase09.1-e2e/**/*.spec.mjs']`

The webServer, workers, reporter, and projects sections remain unchanged.

After editing playwright.config.mjs, verify the new tests are discovered:
`cd /Users/gleicon/code/go/src/github.com/gleicon/webclaw/test && npx playwright test --list --project=chromium 2>&1 | grep "phase09.1"`
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/go/src/github.com/gleicon/webclaw/test && npx playwright test --list --project=chromium 2>&1 | grep -c "phase09.1"</automated>
  </verify>
  <done>Output shows at least 5 test names from phase09.1-e2e/ directory. playwright.config.mjs updated to include both test directories.</done>
</task>

</tasks>

<verification>
1. `ls /Users/gleicon/code/go/src/github.com/gleicon/webclaw/test/phase09.1-e2e/` shows two spec files.
2. `cd /Users/gleicon/code/go/src/github.com/gleicon/webclaw/test && npx playwright test --list --project=chromium 2>&1 | grep phase09.1` shows 10+ test entries.
3. No syntax errors in spec files: `node --input-type=module < test/phase09.1-e2e/01-connected-services-smoke.spec.mjs` (import check).
</verification>

<success_criteria>
- Two spec files exist in test/phase09.1-e2e/
- playwright.config.mjs discovers both old and new tests
- All 13 behavioral requirements from VALIDATION.md have a corresponding test case
- Tests are written to fail until implementation is complete (correct Wave 0 behavior)
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-oauth-ux-api-token-rework/09.1-01-SUMMARY.md`
</output>
