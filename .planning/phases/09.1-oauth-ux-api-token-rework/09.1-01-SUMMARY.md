---
phase: 09.1-oauth-ux-api-token-rework
plan: 01
subsystem: testing
tags: [playwright, e2e, dom-assertions, wave0, oauth, pat, connected-services]

# Dependency graph
requires:
  - phase: 09-social-integrations
    provides: OAuth Connected Services UI, provider card HTML structure

provides:
  - Playwright Wave 0 test scaffold for Phase 9.1 DOM contracts
  - 8 DOM smoke tests asserting card structure, badges, and callout content
  - 5 PAT save flow tests asserting WASM export presence and masked-state behavior
  - Updated playwright.config.mjs covering both phase06 and phase09.1 test suites

affects:
  - 09.1-02: Go backend changes (savePATToken, markInvalid exports)
  - 09.1-03: index.html DOM rework (element IDs, badges, callouts)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Wave 0 test scaffold: write failing tests before implementation to drive DOM contracts"
    - "ES module spec files (.mjs) for new test suites; .js for legacy phase06 tests"
    - "testDir='.' with testMatch glob includes multiple test suites in one Playwright config"

key-files:
  created:
    - test/phase09.1-e2e/01-connected-services-smoke.spec.mjs
    - test/phase09.1-e2e/02-pat-save-flow.spec.mjs
  modified:
    - test/playwright.config.mjs

key-decisions:
  - "testDir changed to '.' with testMatch glob (Option A) — zero risk to existing phase06 tests"
  - "markInvalid test uses test.skip() with documented manual verification path per VALIDATION.md"
  - "Smoke tests do NOT wait for window.webclaw — DOM-structure-only assertions reduce flakiness"
  - "PAT masked-state assertion is dual: accepts either #oauth-masked-{id} visible OR input type=password with empty value"

patterns-established:
  - "Pattern 1: navigateToConnectedServices helper reused across all settings-tab tests"
  - "Pattern 2: test.skip() with comment explains manual path for tests blocked by UI auto-refresh constraints"

requirements-completed: [AUTH-UX-01, AUTH-UX-02, AUTH-UX-03, AUTH-UX-04, AUTH-UX-05, AUTH-UX-06]

# Metrics
duration: 8min
completed: 2026-03-07
---

# Phase 9.1 Plan 01: Test Scaffold Summary

**Playwright Wave 0 scaffold with 13 DOM-contract tests (8 smoke + 5 PAT flow) asserting element IDs, auth-type badges, redirect callouts, and WASM export presence before any implementation begins.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-07T13:58:21Z
- **Completed:** 2026-03-07T14:06:44Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Created `test/phase09.1-e2e/` directory with two Playwright spec files
- 8 DOM smoke tests assert card structure for all four providers (GitHub, Notion, Twitter, Google) without waiting for WASM load
- 5 PAT save flow tests verify WASM export presence (`savePATToken`, `markInvalid`), PAT save → masked state, and markInvalid behavior
- Updated `playwright.config.mjs` from single-dir to multi-suite via testMatch glob — existing phase06 tests unaffected

## Task Commits

1. **Task 1: DOM smoke spec** - `b5c5c83` (test)
2. **Task 2: PAT save flow spec + config update** - `5aa635d` (test)

## Files Created/Modified

- `test/phase09.1-e2e/01-connected-services-smoke.spec.mjs` - 8 DOM structure tests for all 4 provider cards
- `test/phase09.1-e2e/02-pat-save-flow.spec.mjs` - 5 PAT flow tests: export presence, save flow, masked state, markInvalid
- `test/playwright.config.mjs` - testDir changed to '.' with testMatch glob for both suites

## Decisions Made

- Used Option A (single config with testMatch glob) over Option B (separate config file) — zero risk to existing tests, single `npx playwright test` command runs everything
- Smoke tests intentionally do NOT wait for `window.webclaw` — faster, reduces WASM startup flakiness, tests DOM structure only
- Masked-state assertion accepts two valid UI patterns: `#oauth-masked-{id}` visible OR input `type=password` with empty value (allows implementation flexibility)
- `markInvalid` test uses `test.skip(true, ...)` with the full manual verification path documented inline — automated assertion requires UI auto-refresh which may not exist

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

- Wave 0 scaffold complete; all 13 tests are in FAIL/SKIP state (correct behavior before implementation)
- Plan 02 (Go backend: savePATToken, markInvalid, AuthType field) will turn the export-presence tests green
- Plan 03 (index.html DOM rework: element IDs, badges, callouts) will turn the DOM smoke tests green
- Verify command for all subsequent tasks: `cd test && npx playwright test --grep "smoke" --project=chromium`

---
*Phase: 09.1-oauth-ux-api-token-rework*
*Completed: 2026-03-07*
