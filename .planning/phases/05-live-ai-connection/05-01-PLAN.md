---
wave: 1
depends_on: []
autonomous: false
requirements:
  - PROV-01
  - PROV-02
  - SEC-02
---

# Plan 05-01: Async Keystore Initialization

**Goal:** Implement asynchronous key retrieval from encrypted keystore with goroutine-based async patterns.

**Success Criteria:**
- Keystore opens successfully from IndexedDB v5
- Keys are retrieved and decrypted using fixed passphrase "webclaw-v1-key"
- No blocking of main thread during key operations
- Errors are handled gracefully (missing keys, decryption failures)

## Tasks

<task>
  <id>1</id>
  <description>Create async key loader function in main.go</description>
  <file>cmd/webclaw/main.go</file>
  <verify>Function exists with signature: loadProviderKeysAsync(router *provider.Router)</verify>
</task>

<task>
  <id>2</id>
  <description>Implement goroutine-spawning pattern for keystore initialization</description>
  <file>cmd/webclaw/main.go</file>
  <lines>after line 65</lines>
  <verify>Uses go func() { ... }() pattern with channel-based result handling</verify>
</task>

<task>
  <id>3</id>
  <description>Add sequential provider key retrieval with error tolerance</description>
  <file>cmd/webclaw/main.go</file>
  <lines>inside loadProviderKeysAsync</lines>
  <verify>Iterates through ["anthropic", "openai", "openrouter"] with continue-on-error logic</verify>
</task>

<task>
  <id>4</id>
  <description>Implement passphrase constant and key existence check</description>
  <file>cmd/webclaw/main.go</file>
  <verify>Uses const passphrase = "webclaw-v1-key" and ks.KeyExists() before RetrieveKey()</verify>
</task>

<task>
  <id>5</id>
  <description>Add memory clearing after key retrieval (best effort)</description>
  <file>cmd/webclaw/main.go</file>
  <verify>Calls keystore.ClearKey(key) after each successful retrieval</verify>
</task>

<task>
  <id>6</id>
  <description>Add console logging for key load status</description>
  <file>cmd/webclaw/main.go</file>
  <verify>Logs success/failure for each provider with js.Global().Get("console").Call("log", ...)</verify>
</task>

## Must-Haves (for verification)

1. `loadProviderKeysAsync` function exists in main.go
2. Function uses goroutine pattern: `go func() { ... }()`
3. Uses `keystore.NewKeyStore()` to open IndexedDB
4. Iterates through all three providers: anthropic, openai, openrouter
5. Checks `ks.KeyExists()` before attempting retrieval
6. Uses fixed passphrase "webclaw-v1-key"
7. Calls `ks.RetrieveKey(provider, passphrase)` for existing keys
8. Handles errors with `continue` (not fatal)
9. Calls `keystore.ClearKey(key)` after registration
10. Logs status to browser console

## Error Handling

- Keystore open failure: Log warning, return early (router stays empty)
- Key not found: Skip provider silently (continue loop)
- Decryption failure: Log error, skip provider, clear corrupted data if possible
- IndexedDB blocked: Log error suggesting other tabs may be open

## Dependencies

None - this is Wave 1 foundation work.

## Notes

- Pattern follows existing keystore integration in main.go (lines 471-530)
- Must use channel-based IndexedDB operations (see keystore/store.go pattern)
- Do NOT block main thread - all work must be in goroutine
